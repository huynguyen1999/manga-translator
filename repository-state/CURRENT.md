# Current state

Last reviewed: 2026-09-26

## Active architecture migration — behavior-preserving extraction
- The standalone MangaStudio window now composes settings, translator-settings, queue, visual-test, pipeline, theme, and API mixins from `MangaStudio_Data/app/ui/main_window_*`; `TranslatorStudioApp` retains its original entry point and signals.
- DDPM model implementations are split between `ddpm_core.py` and `ddpm_variants.py`; `ddpm.py` re-exports the original class paths and module names.
- DPM-Solver noise schedules, model wrapping, and tensor helpers live in `dpm_solver_utils.py`; the original `dpm_solver` module re-exports them.
- Quadrilateral OCR geometry lives in `manga_translator/utils/generic_quadrilateral.py`; `generic.py` remains the compatibility import surface.
- Shape-aware candidate search lives in `rendering/layout/solve_core.py`; `layout/solver.py` retains its existing search and planning import names.
- Rendering placement geometry lives in `rendering/placement_geometry.py`; `rendering.__init__` remains the compatibility surface.
- Gemini text fallback helpers and ChatGPT text/client helpers now live in `gemini_text.py`, `chatgpt_text.py`, and `chatgpt_client.py`; existing translator names remain importable from their original modules.
- Translation story selection keeps its existing state and behavior in `TranslationSubmitModal`; page previews and split-handle interaction JSX live in `TranslationSubmitParts.tsx`.
- The `/manual` HTML endpoint now lives in `server/api/routes/manual.py`; it is included at its prior registration position.
- FastAPI middleware and route registration now live in `server/app.py:create_app`; `server.main` remains the compatibility runtime and retains the existing endpoint/helper exports.
- Startup, storage selection, scheduler initialization, and shutdown now live in `server/app_lifecycle.py`; `server.main.lifespan` remains a live-runtime compatibility binding so existing runtime patch points stay effective.
- `devscripts/check_line_limits.py` enforces the source-file line-count ratchet in CI: existing files cannot grow past the recorded baseline and new application files are limited to 500 lines. Model, vendored, generated, and test code is excluded; named in-progress extractions are capped at their recorded counts.
- Pipeline rerun presets/validation, checkpoint hydration, stage execution, and artifact commits now live in focused `server/pipeline_rerun_*` modules; `server.pipeline_rerun` retains the public functions and temporary-case monkeypatch seams.
- PostgreSQL batch-manifest writes now live in `server/postgres_batch_persistence.py`; `PostgresBatchStore._save_db_manifest` remains the same injectable compatibility method.
- Single-image lifecycle, model preloading, failure checkpointing, and translation-history updates now live in `manga_translator/pipeline/entrypoint.py`; `MangaTranslator.translate()` keeps its signature and current `save_jpeg` callback seam.
- Bounded image batches, page preparation/rendering, translation-history updates, and runtime cleanup now live in `manga_translator/pipeline/batch/workflow.py`; both public batch methods remain on `MangaTranslator` as delegates.
- Prepared-page lifecycle and memory-recovery flow now live in `manga_translator/pipeline/preparation.py`; `MangaTranslator.prepare()` delegates through the same public method.
- Prepared-page render restoration, completion, and failure cleanup now live in `manga_translator/pipeline/page_render.py`; `MangaTranslator.render()` remains the public entry point.
- Horizontal text measurement and wrapping live in `text_render_horizontal_layout.py`, glyph rasterization in `text_render_horizontal_glyphs.py`, and candidate selection/composition in `text_render_horizontal_composition.py`; `text_render_horizontal.py` and the old `text_render` names remain compatibility facades.
- Vertical text measurement and rasterization now live in `manga_translator/rendering/text_render_vertical.py`; the existing `text_render` attribute surface remains available through a lazy facade.
- Region and editor payload serialization plus text-line/text-block restoration now live in `manga_translator/pipeline/serialization.py`; `pipeline.run` re-exports the same helpers for existing callers.
- Per-stage pipeline retry execution now lives in `manga_translator/pipeline/retry.py`; `PipelineRun.retry_stage()` keeps its signature and resolves patchable dependencies through `pipeline.run`.
- Translation remap geometry and pair scoring now live in `manga_translator/pipeline/translation_remap_geometry.py`; the existing remap module re-exports those helpers and keeps matching orchestration unchanged.
- Rendering font-name/path lookup now lives in `manga_translator/rendering/fonts.py`; package-level imports remain available from `manga_translator.rendering`.
- Safe-mask and rendered-box PNG serialization now lives in `manga_translator/rendering/serialization.py`; `bubble_layout` re-exports the same helper names for existing callers.
- OCR-to-bubble association and source-region snapshots now live in `manga_translator/rendering/grouping.py`; `bubble_layout` keeps these names and its existing restore behavior.
- Bubble semantic breaks, safe line slots, dynamic line fitting, line centering, and positioned glyph rendering now live in `manga_translator/rendering/line_breaking.py`; existing `bubble_layout` names remain available.
- Connected-bubble peak detection, lobe partitioning, geometry profiles, and adaptive font estimates now live in `manga_translator/rendering/lobes.py`; `bubble_layout` keeps the helper imports.
- Bubble-lobe fitting and candidate scoring now live in `manga_translator/rendering/candidates.py`; `bubble_layout` retains the existing candidate helper names.
- Solver workload counters, per-layout caches, timing summaries, and content traces now live in `manga_translator/rendering/layout/profiling.py`; `solver.py` re-exports the existing profiling symbols for callers and tests.
- Bubble row-slot geometry, placement-profile caches, linguistic line-break costs, and DP paragraph flow now live in `manga_translator/rendering/layout/line_breaking.py`; `solver.py` retains the existing helper imports for pipeline and test callers.
- Bubble-gap classification, geometric-obstruction reasons, and vertical paragraph compaction now live in `manga_translator/rendering/layout/paragraph_flow.py`; `solver.py` keeps the existing gap constants and helper names available.
- OCR source-geometry calibration and original typography profile construction now live in `manga_translator/rendering/layout/source_profile.py`; existing solver imports remain available to production and runner callers.
- Region font-size policy calculations now live in `manga_translator/rendering/layout/font_policy.py`; the solver preserves its existing helper names and constants for callers.
- Free-text candidate generation, wrapping, line metrics, ink measurement, and typography scoring now live in `manga_translator/rendering/layout/free_text_typography.py`; translated region bounds are at least the source height and capped at 1.5×; line spacing stays natural, text starts at the top of those bounds, and the paragraph is centered horizontally on the source region. When the available target is wider than the source, wrapping prefers the source width and allows width up to the widest unbreakable word. Page selection solves colliding free-text groups with diverse alternatives and bounded backtracking.
- Inferred bubble assignment rejects bright connected components smaller than the detected text footprint, so white-outlined free text stays on the free-text layout path.
- Free-text damage coverage, panel constraints, overflow checks, offset search, and placement scoring now live in `manga_translator/rendering/layout/free_text_search.py`; solver imports preserve existing helper names.
- Free-text candidate search orchestration, early-accept checks, and result materialization now live in `manga_translator/rendering/layout/free_text_solver.py`; existing solver entry points remain available.
- Bubble glyph-pixel and bounding-box candidate validation now live in `manga_translator/rendering/layout/validation.py`; `solver.py` keeps the helper imports used by the search.
- Bubble-region grouping and geometry-aware zone partitioning now live in `manga_translator/rendering/layout/bubble_zones.py`; the page font baseline helper remains solver-local for its estimator patch point.
- Bubble candidate glyph masks and the composite soft-scoring function now live in `manga_translator/rendering/layout/scoring.py`; scoring constants and solver helper names remain imported unchanged.
- Placement-zone visualization now lives in `manga_translator/rendering/layout/debug.py`, alongside free-text diagnostic visualization; the solver keeps its existing import.
- Bubble line-block centering and horizontal alignment now live in `manga_translator/rendering/layout/centering.py`; the solver retains the same helper imports and search sequence.
- OCR hyphen-split repair and its system dictionary cache now live in `manga_translator/rendering/layout/text_normalization.py`; solver imports preserve the existing normalization names.
- Page-level bubble/free-text orchestration now lives in `manga_translator/rendering/layout/orchestration.py`; `solver.apply_shape_aware_bubble_layout` remains the stable lazy facade and forwards solver-local helpers dynamically.
- Scheduled summary-task error handling and controller cleanup now live in `server/summary_task.py`; `server.main._run_summary_task` preserves its scheduler-facing signature and delegates with the existing callbacks.
- The gallery composition component now lives in `front/app/features/gallery/ResultGallery.tsx`; `front/app/components/ResultGallery.tsx` re-exports it for existing imports.
- The Page Detail modal coordinator now lives in `front/app/features/page-detail/PageDetailModal.tsx` beside its extracted stage, header, tabs, sidebar, and artifact hook; the old component path re-exports its named and default exports.
- Page Detail download/copy, retry/rerender, image navigation, and keyboard actions now live in `front/app/features/page-detail/usePageDetailActions.ts`; modal state and presentation remain in the coordinator.
- Page Detail sidebar drag-resize lifecycle now lives in `front/app/features/page-detail/usePageDetailSidebarResize.ts`; the modal coordinator is under 500 lines.
- Gallery sort, status, selected-manga, and search state plus their route-facing handlers now live in `front/app/features/gallery/useGalleryFilters.ts`; `ResultGallery` still owns gallery page correction and route synchronization.
- Gallery group assembly, selected-group loading, and reader image selection now live in `front/app/features/gallery/useGalleryMangaGroups.ts`; memo and loading-effect dependencies remain unchanged.
- Gallery modal composition now lives in `front/app/features/gallery/GalleryOverlays.tsx`; existing modal state and callbacks remain owned by `ResultGallery`.
- Gallery filtering, move-target search, page counts, and review counts now derive in `front/app/features/gallery/useGalleryGroupViews.ts`, preserving the existing search and status-filter rules.
- Page-level joint candidate selection, collision checks, and candidate geometry helpers now live in `manga_translator/rendering/layout/joint_layout.py`; existing solver helper names remain available.
- Hyphen dictionary selection now lives in `manga_translator/rendering/hyphenation.py`; `text_render.select_hyphenator` and the language list remain available.
- Manga row/accordion rendering now lives in `front/app/features/gallery/MangaRowGroup.tsx`; gallery state and actions remain in `ResultGallery`.
- Gallery card/row library composition now lives in `front/app/features/gallery/GalleryLibraryView.tsx`; filtering, page state, actions, and pagination remain in `ResultGallery`.
- Gallery group assembly from summaries, cached pages, and session pages now lives in `front/app/utils/resultGallery.ts`, re-exported through `ResultGallery` for compatibility.
- Server batch event subscription, optimistic-state reconciliation, completion cache invalidation, and detail updates now live in `front/app/features/batches/useServerBatchEvents.ts`; `App` retains the state and refs. Batch completion refresh and duplicate-snapshot behavior are covered in `batchSync.test.ts`.
- Correlation-ID and original-import serialization middleware now live in `server/api/middleware.py`; `server.main` retains middleware registration names and the import-lock alias.
- Studio translation selection, group confirmation, batch construction, and upload handoff now live in `front/app/features/translation/translationSubmission.ts`; translation batch upload recovery, progress, deduplication, and failure handling now live in `front/app/features/upload/translationBatchUpload.ts`. App retains modal state and shared upload tracking. Contracts cover selection, state handoff, group/story metadata, color flags, persistence order, upload recovery, and failure state.
- The Studio view composition now lives in `front/app/features/studio/StudioWorkspace.tsx`; `App` supplies the same settings and upload handlers, while the component keeps the existing markup, conditions, and classes.
- Studio settings hydration, IndexedDB restoration, and initial server-batch recovery now run in `front/app/features/studio/useStudioInitialization.ts`; `App` still supplies the same setters, refs, and upload-resume callbacks.
- Gallery summary state, page cache/request tracking, server group-title loading state, and route-triggered summary loading now live in `front/app/features/gallery/useGalleryData.ts`; App keeps its existing mutation and batch callbacks.
- Original-manga file validation, multipart import, duplicate-title recovery, upload progress/error state, and modal actions now live in `front/app/features/upload/useStudioMangaUpload.ts`; successful uploads finish at the gallery import endpoint without submitting to the image batch scheduler. App retains its modal state, shared upload ref, form reset, and page-restoration action.
- Studio file removal, range selection, select-all, and deselection now live in `front/app/features/upload/studioFileSelection.ts`; shared state and cleanup refs stay App-owned. The contract covers set/map cleanup and selection-anchor resets.
- Studio color exclusion, selected-file bulk toggles, queued-page color updates, and bounded auto-detection now live in `front/app/features/upload/useStudioColorActions.ts`; the detector still processes four files concurrently.
- Gallery page/group update, reorder, restore, clear, and delete actions now live in `front/app/features/gallery/galleryMutations.ts`; `ResultGallery` receives the same handlers and App retains the state/cache. Contracts cover request bodies, local updates, counts, and cache invalidation.
- Single-manga page sort, optimistic reorder callbacks, duplicate-name detection, and dirty-state derivation now live in `front/app/features/gallery/useGalleryPageOrdering.ts`; `ResultGallery` retains page state and the same reset/drag-scroll effects.
- Stable card and row callback adapters now live in `front/app/features/gallery/useGalleryCardActions.ts`; callback names, dependencies, and their existing consumers remain unchanged.
- Existing manga-group choices for assignment modals now come from `front/app/utils/groupTitles.ts`; the `GroupSelectionModal` type exports remain compatible.
- Selected-page and selected-manga deletion actions now live in `front/app/features/gallery/bulkDeletionActions.ts`; `ResultGallery` retains selection/modal presentation and state. The gallery test exercises deduplication, callback payloads, local cleanup, and detail-close behavior.
- Drag reordering, sort selection, optimistic page-order updates, rollback, and save errors now live in `front/app/features/gallery/pageOrderActions.ts`; `ResultGallery` still owns page-order state and passes the same handlers to the page grid.
- Gallery rename submission now lives in `front/app/features/gallery/renameActions.ts`, including read-progress key migration and loaded-page, expansion, selection, and route updates; rename state and modal presentation remain in `ResultGallery`.
- Manga page ordering, metadata updates, and group deletion now run through `server/services/manga_mutation.py`; HTTP routes retain their existing URLs, response bodies, and backend-specific error handling.
- Gallery editor saves now reconcile loaded page groups, selection, review-only filtering, and overlay closure through `front/app/features/gallery/editorSave.ts`; `ResultGallery` retains editor modal ownership.
- Moving selected pages between manga groups now lives in `front/app/features/gallery/movePages.ts`; it retains the same metadata callback, optimistic loaded-page placement, and selection/modal cleanup.
- Studio lightbox preview image construction now lives in `front/app/features/studio/buildPreviewImage.ts`; `App` still owns selected-image/modal state and retry behavior.
- IndexedDB-restored Studio file/status/selection merges now live in `front/app/features/studio/applyRestoredStudioState.ts`; `App` keeps the same hydration and error timing.
- Server batch and active-upload startup restoration now lives in `front/app/features/batches/restoreServerBatches.ts`; `App` starts it in the same initialization effect and continues to provide upload-resume callbacks and shared refs.
- Text-case normalization, frozen-layout hydration/persistence, bubble detection coordination, and final page rendering now live in `manga_translator/rendering_stage.py`; `MangaTranslator._run_text_rendering()` remains the compatibility facade.
- Detection metadata and reader-thumbnail artifact synthesis now live in `server/result_artifacts.py`; `server.main` retains both helper names as compatibility wrappers and injects existing file resolvers/logging.
- The internal executor `/register` endpoint now lives in `server/api/routes/instances.py`; `server.main` keeps the `register_instance` handler import and includes the router with the same nonce and executor registry.
- Gallery loading, review-clear, and empty-library presentation now live in `front/app/features/gallery/GalleryStatusStates.tsx`; `ResultGallery` still selects states and owns the same callbacks.
- Card and row views share the extracted gallery no-results presentation and filter-reset callback in `GalleryStatusStates.tsx`; labels and classes are preserved.
- Gallery fallback summary fetching and loading state now live in `useGalleryFallbackSummaries`; parent-provided summaries continue to bypass the fallback request.
- Gallery modal and selected-manga Escape handling now live in `front/app/features/gallery/useGalleryKeyboard.ts`; Escape dismissal precedence and selection clearing remain unchanged. The old unreferenced modal key handler was removed; `PageDetailModal` continues to own active page-navigation keys.
- Manga editor undo/redo stacks, edit commits, checkpoints, resets, and keyboard shortcuts now live in `front/app/features/editor/useEditorHistory.ts`; `MangaEditorModal` retains the same load/save reset points and toolbar callbacks.
- Progress hook registration, ordered pipeline progress/checkpoint handling, and default progress logging now live in `manga_translator/pipeline/progress.py`; `MangaTranslator` retains the existing hook and progress method names.
- MPS device-cache coordination, exclusive engine unloads, and TTL cleanup now live in `manga_translator/pipeline/lifecycle.py`; existing `MangaTranslator` lifecycle methods delegate with the same runtime patch points.
- File-backed result-page snapshot construction and its date/natural-sort helpers now live in `server/result_snapshot.py`; `PostgresStore._page_snapshot` and the module helper names remain compatible.
- Gallery synopsis loading/generation, summary-job subscription updates, pause/resume/stop controls, and copy actions now live in `front/app/features/gallery/useMangaSummaryActions.ts`; `ResultGallery` still owns the modal state and renders the same controls. The contract covers summary requests and job-control state updates.
- The single-manga detail header/action banner and reorderable page grid now live in `front/app/features/gallery/MangaDetailHeader.tsx` and `front/app/features/gallery/MangaPagesGrid.tsx`; `ResultGallery` passes through the same state and callbacks while retaining route and modal ownership.
- Page Detail timing, localization, story analysis, and step settings panels now live in `front/app/features/page-detail/tabs/`; shared audit types live in `ProfessionalAudit.ts`, while manifest loading, tab selection, and retry callbacks remain in `PageDetailModal` and `PipelineDetailsSidebar`.
- Page Detail bubble-mask discovery, pipeline-manifest fallback loading, and translation-audit artifact loading now live in `front/app/features/page-detail/usePageDetailArtifacts.ts`; `PageDetailModal` retains the same per-image state presentation.
- Paginated gallery manga-title loading and in-flight request deduplication now live in `front/app/features/gallery/useServerMangaGroupTitles.ts`; the existing preload and modal-triggered calls remain in `App`.
- Route-driven manga-summary fetching, gallery paging cache updates, and stale-response suppression now live in `front/app/features/gallery/useMangaSummaries.ts`; App still owns the state and gallery mutation facade.
- The queue's per-page status, preview, download, and actions row now lives in `front/app/components/QueueItemRow.tsx`; `ImageQueue` retains the same props, markup, and event handlers.
- Studio image cards now live in `front/app/components/ImageHandlingCard.tsx`; `ImageHandlingArea` still owns card ordering, selection state, and upload controls, while card markup and interaction callbacks remain unchanged.
- Pre-load archive/file ordering UI and drag auto-scroll state now live in `front/app/components/PendingFilesOrderPanel.tsx`; `ImageHandlingArea` passes through the existing reorder, remove, clear, and confirm callbacks.
- Summary job row rendering, progress, and action controls now live in `front/app/components/SummaryJobRow.tsx`; `JobsDrawer` retains the same grouping and action callbacks.
- Series creation modal presentation now lives in `front/app/components/CreateSeriesModal.tsx`; `SeriesLibrary` retains selection, ordering, validation, and create actions.
- Reader opening, series-member navigation, adjacent-member preloading, reader close-position capture, and reader-to-editor handoff now live in `front/app/features/gallery/useMangaReaderActions.ts`; reader modal state, deep-link sync, and keyboard behavior stay in `ResultGallery`.
- Reader page image loading, original/translated switching, retry, and single/infinite page presentation now live in `front/app/features/reader/PageItem.tsx`; `MangaReaderModal` retains its existing helper and type exports.
- Reader page-width and single-page-fit controls now live in `front/app/features/reader/ReaderWidthMenu.tsx`; `MangaReaderModal` keeps the same state, callbacks, markup classes, and labels.
- Reader preload windows and series-member navigation now live in `front/app/features/reader/readerUtils.ts`; `MangaReaderModal` re-exports the helper functions for existing callers.
- Infinite-scroll and single-page reader content now live in `front/app/features/reader/ReaderPageStage.tsx`; the modal passes its existing refs, state, callbacks, and edit handler without adding wrapper markup.
- App navigation, title, Studio IndexedDB, and settings persistence effects now live in `front/app/features/studio/useAppPersistence.ts`; effect order, debounce, and dependency lists are preserved.
- Pipeline rerun selection, execution, resulting batch insertion, and rerender entry handling now live in `front/app/features/batches/usePipelineRerunActions.ts`; `App` keeps the same dialog and gallery callbacks.
- Studio preview image construction and its Page Detail modal state now live in `front/app/features/studio/useStudioLightbox.ts`; preview settings and existing close/navigation callbacks remain the same.
- Reader return-position scrolling, reduced-motion handling, and temporary item highlighting now live in `front/app/features/gallery/useRestoreReaderScroll.ts`; `ResultGallery` supplies the same state and keeps the same double-frame timing.
- Gallery series loading, create/add requests, selected-title ordering, and success-toast timing now live in `front/app/features/gallery/useGallerySeriesActions.ts`; `ResultGallery` retains gallery selection, deletion, and modal presentation.
- Saved editor text-region deserialization now lives in `front/app/features/editor/serialization.ts`; the editor keeps image loading, normalization, state, and presentation.
- Editor background source selection, text-region loading, starter-block fallback, and dimension/font normalization now live in `front/app/features/editor/loadEditorDocument.ts`; the modal retains React state updates, history reset, and selection.
- Editor pointer move/resize state and geometry now live in `front/app/features/editor/useEditorDrag.ts`; `MangaEditorModal` keeps the same canvas event wiring and uses the same history commits.
- Editor PNG/JSON exports and save-state handling now live in `front/app/features/editor/useEditorExports.ts`; the existing save endpoint and request body are centralized in `saveEditorEdits.ts`. `MangaEditorModal` is under 500 lines.
- Editor review-count guidance and draft/approval controls now live in `front/app/features/editor/EditorReviewFooter.tsx`; save logic and review-state mutations remain in `MangaEditorModal`.
- Page Detail’s image/view controls and page actions now live in `front/app/features/page-detail/PageDetailHeader.tsx`; the modal retains state, callbacks, and image/sidebar orchestration.
- Gallery sorting/selection/read-progress helpers, series selection dock and creation toast, single-image and CBZ downloads, shared page-selection actions, page/manga/row cards, selection state, drag auto-scroll, on-demand manga page loading, studio file intake and pending archive queue, header controls, move/delete/create-series dialogs, and the saved-manga synopsis modal now live in feature modules. Editor canvas stage, toolbar, geometry, and inspector tabs plus Page Detail panels, translation job rows/thumbnails, batch cards/actions, virtualized batch page lists, batch and summary action callbacks, app route/navigation state, and translation option state/persistence now use feature or utility modules with existing imports preserved. Server health, batch, CBZ export, direct translation, editor, internal batch translation, original-manga import, manga-list/page, manga management, pipeline-manifest, pipeline-rerun, result-file, result-page, summary, reader-progress, series, and translation-batch routes plus summary, manga, series, batch, editor, and pipeline request schemas now live under `server/api`; series, summary, pipeline-state/artifact persistence, manga/page queries, and virtual document persistence live in `server/series_repository.py`, `server/summary_repository.py`, `server/pipeline_repository.py`, `server/manga_repository.py`, and `server/document_repository.py` behind `PostgresStore`. Original-manga upload validation, archive filtering, image normalization, filesystem staging, and preview warmup now live in `server/original_import.py`, with the prior helper names retained in `server.main`. File-backed metadata, review state, and page-order helpers now live in `server/result_metadata.py`; result and manga-group scans now live in `server/result_queries.py`, with existing `server.main` imports preserved. Summary OCR setup, source-text repair, and single/batch OCR calls live in `server/summary_ocr.py`, with the prior `server.main` helper names preserved; manga-wide OCR stage scheduling lives in `server/summary_ocr_execution.py`. Active summary and OCR job controls live in `server/summary_jobs.py`, with `SummaryJobController` retained from `server.main`; summary job orchestration lives in `server/summary_generation.py`, with `server.main._generate_manga_summary` retained as the runtime-injected compatibility entry point. The PostgreSQL batch adapter lives in `server/postgres_batch_store.py` and remains importable from `server.postgres_store`; semaphore limits and acquire/release behavior live in `server/batch_resources.py`, while checkpointed OCR and model-stage execution live in `server/batch_stage_executor.py` behind the same `BatchScheduler` methods. Detection, OCR, bubble detection, image preprocessing, inpainting, translation validation/context/dispatch/retries, text grouping, text translation, and post-translation processing now live in focused stage/helper modules. `MangaTranslator` retains the existing method surfaces and `TranslationFailure` remains importable from its prior module. `server.main` retains the app entry point and schema/handler names for compatibility. `test/refactor_contract` protects backend imports, route paths/order, response shapes, summary helper imports, and pipeline stage order; `front/app/refactor-contract` protects route registration plus upload, gallery, batch-stream, and summary-control flows.
- Translation validation and single-region retry logic now live in `manga_translator/translation_validation.py`; existing async method surfaces remain on `MangaTranslator`.
- Compatible OCR and page inference group selection now lives in `server/batch_inference_groups.py`; standard preparation and checkpointed model-stage execution live in `server/batch_stage_executor.py`. `BatchScheduler` retains its existing methods and call sites.
- Batch stage ordering, translation readiness, queued-item selection, and story-plan remapping now live in `server/batch_group_selection.py`; the previous scheduler helper methods remain as compatibility delegates.
- Atomic page, rerun, translation-group, and legacy item claims now live in `server/batch_claims.py`; `BatchScheduler` keeps each existing `_claim_*` method.
- Persisted batch settings are mapped to `Config` in `server/batch_config.py`; `BatchScheduler._config_for` remains the tested facade method.
- `ResultGallery` delegates route-driven Page Detail and editor modal synchronization to `front/app/features/gallery/useGalleryPageModalRoutes.ts`; route props and current component state ownership remain unchanged.
- `PostgresStore.sync_result_folder()` delegates filesystem-backed page, document, artifact, and pipeline-manifest indexing to `server/result_indexing.py`, preserving the facade method and its snapshot override surface.
- `PostgresStore.get_progress()` and `save_progress()` delegate reading-progress queries and writes to `MangaRepository`, preserving the store API.
- Page identity, request/folder resolution, text-region persistence, review status, and group existence/title queries now live in `server/page_state_repository.py`; `PostgresStore` keeps the same facade methods.
- PostgreSQL connection lifecycle and schema migrations now live in `server/postgres_schema.py`; untracked-result scans live in `server/result_index_queries.py`, and document ownership resolution lives in `server/document_owner.py`. `PostgresStore` remains the compatibility facade.
- Summary status reads and job updates now live in `server/summary_status.py`; `server.main` retains the helper names and selects the same PostgreSQL or file-backed path.
- Batch pause/resume, dismissal and group compaction, translator/title/priority/manual-review updates, retry validation/rescheduling, page removal, and drain-before-delete now live in `server/batch_mutations.py`; `BatchScheduler` retains the same methods and transition helper.
- Runnable-batch selection, claim dispatch, task creation, and running-item cleanup now live in `server/batch_dispatch.py`; `BatchScheduler._launch_available()` remains the compatibility entry point.
- Pipeline stage progression now lives in `server/batch_stage_progression.py`; memory-pressure inference limits now live in `server/batch_memory_limits.py`; `BatchScheduler` preserves its existing methods as delegates, while semaphores remain in `server/batch_resources.py`.
- Textless checkpoint completion now lives in `server/batch_checkpointed_prepare.py`; `BatchScheduler._complete_checkpointed_textless()` remains the compatibility and test patch point.
- PostgreSQL and file-backed manga page ordering and title updates now pass through `server/services/manga_mutation.py`; HTTP routes keep status mapping and response construction.
- Translation batch detail request deduplication and optimistic translator reconciliation now live in `front/app/features/batches/loadBatchDetails.ts`; `App` retains the callback and shared state refs.
- Per-item translation completion, failure persistence, checkpoint resume, and executor cleanup now live in `server/batch_item_runner.py`; atomic pipeline reruns live in `server/batch_pipeline_rerun.py`; checkpointed prepare-stage lifecycle lives in `server/batch_checkpointed_prepare.py`; stage-barrier translation groups live in `server/batch_translation_group.py`. `BatchScheduler` retains the existing method surfaces.
- Worker nonce/environment setup, CPU sizing, in-process and subprocess worker setup, and subprocess supervision live in `server/worker_runtime.py`; resource sizing and startup selection live in `server/server_preparation.py`, while `server.main.prepare()` remains a runtime-injected compatibility wrapper.
- PostgreSQL page ordering, manga metadata/group updates, and result deletion now live in `server/manga_mutation_repository.py`; `PostgresStore` preserves the existing methods and page-order helper surfaces.
- `MangaTranslator` image-context tracking, result metadata assembly, and output-path selection now live in `manga_translator/pipeline/context.py`; its existing helper methods remain delegates.
- Pre-translation orchestration now lives in `manga_translator/pipeline/orchestrator.py`; `_translate_until_translation` remains the compatibility entry point, with its stage order characterized by a focused test.
- Batched detection/OCR/text-region extraction now lives in `manga_translator/pipeline/batch/text_extraction.py`; `MangaTranslator.extract_text_batch()` keeps its public method and utility patch points.
- Bounded batch coordination, batch mode selection/fallback, and concurrent/text translation helpers now live in `manga_translator/pipeline/batch/`; the single-image translation pipeline now lives in `manga_translator/pipeline/single_page.py`. `MangaTranslator.translate_batch_contexts()` retains its method surface.
- Post-translation completion and final upscale reversion/result-artifact serialization now live in `manga_translator/pipeline/completion.py` and `manga_translator/pipeline/finalization.py`; MPS fallback and its standalone lock live in `manga_translator/pipeline/model_execution.py`. `MangaTranslator` retains the existing entry points and module-level lock aliases.
- This migration does not intentionally change API routes, UI styling/interactions, pipeline ordering, or layout algorithms. Layout helper responsibilities have been extracted into focused submodules; the solver keeps its compatibility imports and stable `apply_shape_aware_bubble_layout` entry point.

## Gallery detail links
- Page and manga cards use canonical route links, so browser Command-click and Copy Link work normally. Page Detail copies the canonical page URL.

## Backend UI surface
- The API server no longer serves the legacy HTML translator at `/`; port 8000 serves API routes and OpenAPI docs at `/docs`. The separate browser Studio remains under `front/` on port 6868.

## Reader mobile navigation
- Touch readers use a dedicated, full-width page jump row with a numeric keyboard and 44px controls; desktop keeps its compact toolbar input.

## Active feature — original manga CBZ export
- Manga library downloads offer translated and original CBZ archives. Original exports use each page's retained source image and name the archive with an `_original` suffix.

## Active optimization — page-local layout profiling and reusable free-text rasters
- Solver counters now belong to one layout execution. The line-alpha cache includes the active font selection; candidate ink, visual, and block masks are reused across placement offsets, and final QA rasterizes overflow from those cached masks.
- Row-slot, placement-target, and zone-profile results are cached for the lifetime of one page layout. Long-word pressure reads a cached row maximum or computes only that maximum; hyphenation rescue skips splits that cannot fit or improve the bottleneck.
- Free-text search retains the existing score and exhaustive fallback while deferring full QA until the best eight candidates. Empty-ownership regions skip candidate generation and suppressed free-text failures request review. Damage-scope dilation uses geometry-calibrated source font estimates. Strict ideal placements are attempted by default; `LAYOUT_FAST_FREE_TEXT=0` disables the ideal stage, and `LAYOUT_LAZY_CANDIDATES=1` independently enables local candidates. Multiple free-text regions use exact joint collision checks and rerun exhaustive search for colliding fast placements. `--layout-shadow-compare` returns the exhaustive layout. Bright-component bubble inference is a fallback when detection is disabled or returns no bubbles; when detections exist, unmatched text stays on the free-text path. Row-slot bands use vertical prefix counts; DP word-run widths use prefix sums, compaction visits only feasible Y positions, and composite whitespace scoring reuses row/column counts.
- `python devscripts/pipeline_step_runner.py layout-benchmark --dataset devscripts/data --repeat 5` reports layout percentiles and solver counters by page category. The current local dataset contains one sample page; shadow comparisons on a larger corpus should quantify candidate-quality differences.

## Active developer tool — local semantic-search lab
- `devscripts/semantic_search_lab.py` indexes manga directories and ZIP archives with selectable pinned BGE, Qwen3-Embedding-0.6B, or GTE-base text encoders plus SigLIP image embeddings, normalized `.npy` vectors, incremental fingerprints, exact cosine retrieval, image/text/RRF comparison, standalone HTML reports, and labeled benchmarks. Select `--text-model bge|qwen3|gte` consistently when indexing and searching; each model needs its own summary vectors. `retrieve` returns ranked summary chunks with cosine scores and source offsets as JSON, without generation. Combined results rank by RRF and show text and image cosine scores separately because those vector spaces cannot be compared directly. `index --mode summary` and `index --mode image` can build either modality first while preserving compatible caches. CLI search defaults to five results and prints up to 1,000 characters of matching summary context. The thin Colab/Jupyter frontend is `devscripts/semantic_search_lab_colab.ipynb`; no database or server is required.
- `devscripts/export_manga_for_search.py` exports manga pages and summaries directly from a running manga translator server into the required directory structure for `semantic_search_lab.py`.

## Active developer tool — manga panel detection & segmentation (YOLO26)
- `devscripts/detect_panels.py` runs inference with both `ShadowB/Manga109-panel-balloon-text-yolov26-segmentation` (YOLO26s instance segmentation for panels, balloons, and text with polygon masks) and `leoxs22/manga-panel-detector-yolo26n` (YOLO26n panel and text detector). It handles automatic checkpoint downloading from Hugging Face, Ultralytics serialization shims (`Segment26`, `Proto26`, `E2ELoss`), RTL/LTR reading-order sorting, retina mask overlays, panel cropping, and metadata JSON generation.



## Active feature — memory-aware shared model cache
- The shared executor tracks each cached model's last use and active users. Idle entries expire after the server's 120-second default, with `--models-ttl 0` retaining them indefinitely; under 60% process-RAM pressure it evicts idle entries by LRU, and at 72% it performs full idle eviction and synchronized device-cache cleanup.
- Model unload paths invoke backend unload hooks, including detector reference cleanup. Bubble model identity is checkpoint/device only, with confidence, mask threshold, and image size passed per inference.
- Detection no longer copies unmodified source pages. 48px OCR and MangaOCR generate crop microbatches instead of retaining all page crops; scheduler page groups cap at 3 for detection/OCR/bubbles and 2 for upscaling/inpainting, with smaller groups as process RSS approaches the memory threshold.

## Active feature — software-rendered Studio overlays
- Jobs and Page Detail render in a shared portal above an inert, scroll-locked app root; blur is scoped to the viewport-sized overlay. Jobs rows use 160px WebP thumbnails and expanded lists over 16 pages render visible rows plus overscan with measured heights. Page Detail uses reader-sized WebP variants through 150% zoom, switches to full images above that, and aligns overlays from pipeline source dimensions; it mounts only the selected view (two images for Split). The memoized image stage coalesces geometry reads to animation frames, the details sidebar owns its tab and retry-selection state, and sidebar drag width applies through a CSS variable until release. SSE batch merges preserve unchanged batch and item references. In development, `?renderPerf=1` exposes `window.__renderPerf.reset()` and `.report()` for frame pacing, p95 frame time, click response, long tasks, React commits, Chromium long-frame layout/render timing, and acceptance thresholds.

## Active feature — searchable manga assignment
- Translation and upload group dialogs search existing manga titles; Gallery's Move to Manga dialog filters its existing manga list. Studio existing-group options come from library manga, not recent titles or unfinished batches.

## Active feature — restore finished batch pages
- Manga detail can reassign saved result folders from terminal translation batches matching the manga title or group ID into the open manga group. Translation completion invalidates cached manga page lists.

## Active feature — speech bubble preview
- Page detail loads saved `bubble_detections.json` polygons and outlines detected speech bubbles on the original page, with loading and missing-data feedback.

## Active feature — source typography measurement
- Shared OCR processing measures each raw textline's relative source size, ink density, relative stroke width, orientation, rotation, and OCR foreground/background colors against the original page before inpainting; measurements persist per line through merged region JSON and pipeline manifest reloads. Preserved numeric-only annotations use the measured source font size as their layout target.

## Active behavior — orientation-aware textline grouping
- Textline grouping and its MST split share an orientation-aware pair metric based on perpendicular spacing, flow-axis overlap, font size, and angle. Vertical Japanese columns retain right-to-left order; verbose runs include `textline_merge_debug.json` pair diagnostics.

## Active feature — preset-based pipeline rerun and translation remapping
- Unified partial and full rerun execution into `server/pipeline_rerun.py` (`PipelineRerunMode`: `full`, `typesetting`, `translation_typesetting`, `reprocess_text`) and `server/batch_scheduler.py` (`_process_pipeline_rerun_item`).
- `GET /api/pipeline-cases/{page-ref}/data` returns saved page metadata, settings, manifest, and available stage JSON documents in one read-only request; `devscripts/pipeline_case.py get` uses it for inspection without invoking layout or rendering.
- `devscripts/export_pipeline_step.py --manga <title> --step <artifact> [--sort-order asc|desc]` exports one saved pipeline artifact for each page in the matching manga as JSON.
- Executes only selected canonical stages (`_run_detection`, `_run_ocr`, `_run_textline_merge`, `_detect_speech_bubbles`, `build_inpaint_masks`, `_run_inpainting`, `_run_text_translation`, `_run_text_rendering`) without creating duplicate pipeline runners.
- Reprocess Text mode preserves user-edited or AI translations across detector and OCR changes using geometry-first translation remapping (`manga_translator/pipeline/translation_remap.py`).
- Intermediate outputs stage in an isolated workspace directory (`.rerun/<job_id>`) and commit atomically upon successful execution, leaving live results untouched on failures.
- Editor artifacts retain stable source region IDs and the source font size; legacy typesetting reruns recover those values from saved translations before recalculating layout.
- Web Studio UI exposes `PipelineRerunDialog` with preset cards, collapsible step breakdowns, and context-relevant parameter overrides.

## Pipeline run records
- Pipeline stage boundaries log process RSS, Python GC object count, MPS allocated/driver memory, batch/page identity, and RSS delta at DEBUG level; server console verbosity defaults from `LOG_LEVEL` in `.env` and can be overridden with `--log-level` or `--verbose`. Mask diagnostics are dropped before layout; `Context.cleanup_runtime()` drops saved page buffers and layout scratch on run release while retaining requested result canvases. Final mask aliases avoid one full-page copy, legacy `translate_batch()` prepares bounded chunks, and MPS inpainting is capped at 1024px to reduce full-page float32 activation peaks. Batch completion clears translator page-history state and synchronizes/empties device caches without unloading shared models.
- GPU inference logs identify detection/OCR page groups, MangaOCR crop groups, YOLO bubble-prediction page counts, and actual inpainting tensor batch/device. Upscaler logs distinguish UltraSharp tensor batches from one-item tile calls and NCNN-Vulkan directory-process inputs.
- Server-generated stage manifests provide per-page timing and preserve batch retry/resume data; active batch rows show elapsed time from the persisted stage start, and job cards show the current pipeline stage with the number of pages that have passed it. Repeated progress signals for an unchanged stage do not write a new manifest revision. Standalone Pipeline Lab navigation, upload shortcut, history, and manual stage controls are removed.
- Queued batch rows show the next scheduled pipeline stage when known, or whether they are waiting to start or for a batch slot.
- In-process manga batches use a job-wide stage barrier: every nonterminal page completes the current stage before any page enters the next. The order is colorization, upscaling, detection, OCR, bubble detection, text grouping, translation, mask generation, layout, inpainting, and rendering. Accelerator model calls remain serialized; page groups for compatible 48px OCR (including CTC), MangaOCR, default detection, YOLO bubble detection, and default AOT inpainting target the `--workers` count. OCR crop calls include at least one crop per grouped page and grow beyond 16 rows when needed. MangaOCR batches crops across pages for both text generation and its auxiliary confidence/color model, with generation chunks sized to at least the page-group count. MPS default and DBConvNeXt detection batches run at worker count in one FP16-autocast inference call to limit activation memory; MPS out-of-memory errors remain on the accelerator path instead of retrying on CPU. Inpainting batches only combine shapes with at most 25% padding overhead; LaMa Large and MPE stay single-page for model-memory and positional-encoding limits. CPU-only inference can also batch compatible pages in worker-sized groups. Translation uses its configured page/story groups after all pages clear preparation. Translation documents are checkpointed before mask generation, and post-translation work resumes from persisted page stages. A failed page blocks the batch until retry or removal; completed textless pages count as skipped. A cached inpainted image without its active mask is discarded and rebuilt. Mutable-store stage-barrier batches require an in-process checkpoint-capable executor; remote whole-page workers are not selected for them.
- `manga_translator/pipeline/stages.py` defines canonical stage IDs (including optional colorization), resources, dependencies, progress mapping, settings invalidation, and stable JSON fingerprints. In-process batch scheduling configures resource-class limits from pipeline workers; accelerator-backed model stages serialize through one GPU slot and stay on the configured device while waiting. Background CPU auto-caps at 3 with two slots available when at least two pipelines are configured, or can be set by --cpu-stage-workers; light CPU uses the same budget; network 4; I/O 4. One process-wide background CPU pool enforces the heavy-stage budget across executor loops, with one reserved interactive slot. Layout uses per-thread FreeType state and runs without the page-wide render lock, so independent pages can use those CPU slots concurrently. Default/DBNet detection, including on MPS, batches compatible pages up to the configured `--workers` count. The UI-default 48px OCR, 48px CTC, and MangaOCR paths batch compatible pages up to the same count, including when verbose logging is enabled; compatible YOLO bubble pages and default AOT inpainting use that target. Inpainting tensor batches allow no more than 25% padding overhead. LaMa Large and MPE remain single-page; 32px OCR keeps the single-page path. Pipeline reruns invalidate dependency closures and persist stage attempts for indexed pages in PostgreSQL; the existing page timing endpoint overlays those records. Checkpoint manifests persist layout output and support automatic failed-stage or explicit `fromStage` retry; checkpoint-backed items enter the resume processor through the normal batch scheduler. Retry plans use the dependency graph: Translation retries preserve mask/inpainting outputs, while OCR retries rebuild text grouping from saved bubble geometry and rerun dependent stages. Page timing responses include direct dependencies so per-stage retry previews use the server graph. Scheduler startup closes unfinished database attempts as interrupted.
- PostgreSQL migration `013_pipeline_stages.sql` adds per-page stage state and attempt history plus revisioned structured-document and artifact registries. Indexed reruns persist stage attempts and atomically activate versioned artifact/document revisions; existing final/inpainted/mask URLs serve the active revision. Normal batch result indexing imports finished canonical stage records, recognized structured JSON, and completed disk-artifact metadata into PostgreSQL; other documents stay in the compatibility document store.
- Professional translation groups pages by configured story segments and rebases the selected segment and archive ranges before translation. The batch-wide stage barrier holds all stories when any page fails, until that page is retried or removed.
- PostgreSQL batch mutations lock the batch row across read-modify-write, and batch/item status and stage reads hydrate from relational columns; the manifest remains a compatibility payload. Item stage start times persist in PostgreSQL, so scheduler claims and progress updates serialize and read back consistently across backend processes.

## Active feature — unified canonical layout, mask, and rendering pipeline

- Studio (`MangaTranslator`, `batch_scheduler.py`) and devscripts (`pipeline_step_runner.py`) are unified into a single canonical pipeline.
- Mask generation is unified into `manga_translator/mask_builder.py` (`MaskBundle`, `MaskMetrics`, `build_inpaint_masks`, `recover_bubble_residual_text`, `create_mask_sources_overlay`), computing identical detector rescue, text, bubble residual, and inpaint masks across both batch studio jobs and CLI runner captures.
- Bubble geometry is prepared once as `geometry.PageGeometry` and reused by mask construction and layout. Per-bubble cleanup/edge geometry is crop-local, with the safe interior retained only for current layout and serialization compatibility; one page-wide protected-edge mask is shared.
- `MaskBundle.profile` records refinement, geometry, detector cleanup, residual recovery, composition, and workload metrics; Studio and CLI captures persist it as `profiling.json`.
- Text-component growth and ignored-bubble dilation operate on component crops. Layout debug rendering is opt-in, candidate collision work uses crop masks, and free-text ownership plus region identity preparation each have one shared implementation.
- Canonical inpaint composition treats detector/refined text evidence as authoritative. Bubble preparation now exposes safe geometry plus a narrow source-image outline protection mask; final text growth is component-constrained and protected pixels are restored after inpainting.
- Residual text recovery inside speech bubbles (`recover_bubble_residual_text`) uses dual global & adaptive Gaussian thresholding, safe bubble interior erosion, orientation-aware text envelopes, and connected-component filtering to completely erase OCR-missed characters/punctuation (e.g. trailing `!?`, kana fragments, gray antialiased glyphs) while strictly protecting bubble outer borders (`final_mask ∩ protected_bubble_edge == ∅`).
- Mask diagnostic artifacts (`mask_raw.png`, `text_mask.png`, `detector_rescue_mask.png`, `bubble_residual_mask.png`, `protected_bubble_edge.png`, `inpaint_mask.png`, `mask_final.png`, `mask_sources_overlay.png`) are persisted across Studio debug outputs and devscripts capture step data.
- Speech bubble detections are serialized and persisted to `bubble_detections.json` during batch preparation and reloaded during translation/rendering, eliminating duplicate YOLO inference passes.
- Studio stage serialization (`serialize_regions` and `deserialize_textblocks`) losslessly preserves `source_font_size`, `calibrated_font_size`, `placement_mode`, `source_region_ids`, `source_regions`, and `bubble_safe_shape`.
- Text rendering is unified into the canonical `render_page()` function in `manga_translator/rendering/__init__.py`, guaranteeing bit-level pixel and geometry parity between Web Studio translations and devscripts fast renders.
- `render_page()` partitions restoration from drawing: suppressed regions are restored once before rendering and excluded from all renderers; valid review-required fallback translations still draw.
- FREE_TEXT regions use conservative CV frame-line inference to constrain wrapping and the paragraph box to the source panel, with page bounds as fallback. Source height is a preference when the panel is shorter; candidate search retains intermediate font sizes, widens wrapping before reducing size, and stops at the first size with a valid placement. Paragraphs prefer the source top and horizontal center, keep words intact, and clamp line spacing to 0–25% of font size. Final output below 85% of the calibrated source font is reviewed and suppressed.
- Bubble text tries normal wrapping, then one language-aware split for a 7+ letter bottleneck word. The preferred-range floor is the lower of 90% target or target−2px. If that range still fails, a review-only fit may go down to half target; anything smaller stays suppressed. Explicit font settings remain the target.

## Active migration — production layout boundary

- `manga_translator/rendering/layout/` owns page-layout data contracts, extracted bubble-mask geometry/lobe detection, page obstacle masks, shared free-text ownership targets, region identities, crop-local candidate rasterization, and non-fatal layout validation.
- `layout_page()` runs after final mask generation and before inpainting; it runs the shared shape-aware solver used by the pipeline runner, then freezes the result for rendering and diagnostics.
- Versioned `layout.json` is the authoritative `FrozenLayout`: it stores stable region/bubble IDs, selected styles, safe bubble shape, exact positioned lines, effective stroke width, and fingerprints for the layout algorithm, translations, source geometry, bubble geometry, mask, font, settings, and page size. Rendering rejects stale algorithm revisions, recomputes layout, then rasterizes frozen lines without reflow; `translations.json` remains the text source. Rare unresolved legacy placements retain their validated RGBA crop.
- Source text, source font size, source geometry, and source-region ownership are snapshotted before layout. One strict resolver supplies both layout and rendering: preserved regions use the exact source snapshot, translated regions use only non-empty translations, and review restoration cannot repaint source members owned by a rendered group. Frozen layouts validate render-content checksums and reject duplicate ownership.
- Free text prefers source−1/source−2 sizing without automatic enlargement, validates implausibly small or large OCR sizes against source geometry, stays anchored through source-footprint block overlap, uses protected bubbles as hard obstacles, receives a region-owned local cleanup mask before lettering, and records final font/content/collision/cleanup diagnostics in `layout.json`.
- Bubble layout uses a robust page dialogue baseline as its preferred target; long words do not cap adaptive font estimates, and preserved annotations do not affect the baseline or receive inserted hyphens. The non-hyphenated solve remains primary; one legal split of a 7+ letter bottleneck word may replace it after a meaningful font gain. Legacy multi-lobe fallback applies the same preferred font floor after resetting to the OCR target and flags below-floor fits for review. `no_hyphenation` disables rescue, and comparison diagnostics persist in `layout.json`.
- Inpainting requires erase-mask coverage only for drawable text; reviewed suppressed regions and unchanged translations keep their source pixels. Strict render diagnostics validate the persistent frozen placement and mask after temporary search zones are released.
- Mask growth clips a connected component to bubble interiors only when its OCR source is bubble-associated; nearby free-text source masks remain erasable, with protected bubble edges still excluded. Final raster validation also checks drawables against source text restored for suppressed or unchanged regions.
- The legacy bubble fallback keeps a calibrated source font target instead of inflating it from translated text length; adaptive enlargement remains for legacy input without a calibrated target.
- `TextBlock` and grouped bubble regions preserve `region_id`, `source_region_ids`, and per-source geometry snapshots for ID-based edits and multi-region placement.
- Bubble association persists stable `bubble_id` values so separate OCR regions in one speech bubble remain jointly optimized after checkpoint reload. Typesetting reruns reconnect those IDs to saved detection masks; unmatched regions stay free text when saved detections exist. Merging remains opt-in through `BubbleDetectionConfig.group_regions` or the dev runner's `--bubble-grouping` flag.

## Purpose

Manga Image Translator translates text in manga and other images through a pipeline of detection, OCR, translation, inpainting, and rendering. It supports command-line, desktop, API/server, and browser-based workflows.

## Main areas

- `manga_translator/`: core Python translation pipeline and model integrations.
- `server/`: web API, persistence, scheduling, streaming, search, and summaries.
- `front/`: React 19 and React Router 7 web studio.
- `MangaStudio_Data/`: desktop studio configuration, profiles, UI data, and tasks.
- `devscripts/`: developer evaluation, benchmarking, maintenance utilities, and the pipeline step runner (`pipeline_step_runner.py`, `capture_step_data.py`, `fast_render.py`) for capturing English or raw manga OCR/bubble/inpainting data (with optional translation support via `--sugoi` / `--translator sugoi`) to `devscripts/data` and testing fast text placement, fitting, and rendering (with `--no-bubble-grouping` option to keep individual text regions separate instead of merging them per bubble). Separate regions that share a bubble are laid out jointly with source-anchored placement zones. The shared shape-aware solver (`manga_translator/rendering/layout/solver.py`) enforces a structural **continuous paragraph rhythm** invariant ($y_i^* = y_0 + i H$), bidirectional spring-chain vertical compaction (`_compact_vertical_rhythm` with continuous Y sub-row search and precomputed `_build_row_slot_table`), tightened geometric obstruction checks (`_is_geometric_obstruction`), staged candidate generation and pruning across line-count diversity buckets before expensive geometric refinement passes, bounded ephemeral mask/band cache lifecycles (`clear_ephemeral_caches`), feasibility-bounded block recentering (`_center_layout_block`), explicit adjacent gap classification (`_classify_adjacent_gaps`), and coarse-to-fine hierarchical font sizing and Y-origin search. Level-2 profiling (`SolverProfileStats`) instruments fine-grained layout sub-phase timings and workload counters (DP calls/states/dedup, Y trials, font trials, cache hits/misses, crop rasters) reported in `--solver-report`.
- The `typediag` diagnostic subsystem in `pipeline_step_runner.py` provides standalone font-metrics and typography rendering correctness validation across 10 phases: `render-test` (layout-bypass test with metric guide lines and glyph bounding boxes), `trace` (per-character resolution path and determinism verification), `scale-audit` (AST/source inspection and runtime checks for unauthorized per-character scaling), `cache-audit` (face-keyed glyph cache coverage), `validate` (batch verification of font and size consistency per text region across datasets), and `fixtures` (golden reference visual regression test suite). It establishes the `FontContext` immutable typography configuration dataclass, decoupling requested size from glyph ink height ($H_{\text{ink}} = \text{ascender} - \text{descender}$) and line height ($H_{\text{line}}$). Fast render now resolves one active font for both solver measurement and final rasterization, and `--solver-report` includes per-region typography face/size/fallback diagnostics.
- The pipeline step runner now calls the shared solver and classifies each region once after bubble association: `BUBBLE` keeps the existing `BubbleGeometry → solve_layout()` path, while `FREE_TEXT` uses `FreeTextDamageTarget`/`FreeTextZone` for region-specific inpaint footprints. Free text first generates whole-word, page-independent typography candidates using source font/aspect priors and actual glyph-ink footprint metrics, freezes the chosen paragraph with one natural baseline rhythm, then centers its rendered ink on the source-plus-inpaint target centroid and searches rigid offsets while allowing expansion beyond the source ownership partition into blank page space. Free-text candidates are ranked and bounded (top 16 per region) for fast joint selection without large Cartesian product overhead. Page, bubble-halo, foreign-text, and actual-glyph collision constraints remain hard; target dimensions influence font size and wrapping, never inter-line stretching. `--solver-report` and `free_text_layout_debug.png` expose source/inpaint/target and ink footprints, baseline rhythm, coverage, typography alternatives, and final offsets.
- Non-bubble capture/render state preserves stable region IDs, grouped source ownership, the exact final inpaint mask, and an OCR → translation → layout → render trace; editable region JSON is applied by ID rather than list position.
- Fast render timing now breaks the layout total into mask preparation, placement-mode classification, bubble solving, free-text solving, fallback placement, and uninstrumented remainder in the batch summary, with Level-2 profiling stats.
- The row DP treats leading/trailing whitespace separately from paragraph-internal whitespace: usable rows form a continuous text stack, unavailable bands remain geometry-forced traversals, and excess line-to-line distance is penalized nonlinearly.
- `docs/`: focused technical and experimental documentation.

## Translation pipeline

- Jobs page rows can remove a queued page, skip one waiting for translation, or cancel an independently running page after its current stage finishes; grouped active stages remain untouched. Startup discards batches whose persisted status is stopping.
- Translation settings offer `fast` (the existing batched path) and `professional` Japanese-to-English localization. Professional mode waits for its configured page/story group, analyzes OCR-only story boundaries and structured linguistic register (character voice, dialect, slang, idioms, pronouns, honorifics/forms of address, and localization conventions), uses neutral compact analysis instructions and fidelity-focused draft/editor instructions, applies optional page-range overrides, translates pages sequentially with rolling context (bounded to 8 000 chars in memory), enforces native LLM JSON mode (`response_format` / `response_mime_type`) with resilient cutoff parsing, routes adult-content refusals and JSON-format failures to DeepSeek, runs a separate editor pass, persists `professional_translation.json`, and flags uncertain regions for review. Both modes translate from OCR checkpoints without loading page pixels; mask, layout, inpainting, and rendering run later under the job-wide stage barriers.
- Professional submissions use a two-step group/story dialog. ZIP/CBZ pages retain archive identity, default to one story per archive, support contiguous manual split markers via an interactive visual filmstrip with draggable boundary handles, hover gap split creation, color-coded story bands, a right-side high-resolution page preview and cut-off boundary inspector (side-by-side transition preview), optional OCR-time break detection, and an explicit merge-all-pages mode. The structured `storyPlan` is persisted with each batch and audit document; draft/editor requests use the configured page batch size but never cross story boundaries; the legacy `storyPageRanges` setting remains accepted for compatibility. Pixel-backed page work resumes from persisted inputs and stage artifacts after translation.
- Professional progress is surfaced as `Analyzing story`, `First draft · Story x/y · Chunk a/b`, and `Editor pass · Story x/y · Chunk a/b`; completed page details show the persisted Japanese source, first draft, editor final, confidence, and review reasons from `professional_translation.json`. The first pass is intentionally an accurate working draft, while the editor independently rechecks it for natural localization.
- Page details also expose the persisted professional story analysis: summaries, page boundaries, confidence, structured character voice, honorific policy, language features, localization conventions, relationships, voice notes, continuity, glossary, and ambiguities. The analysis establishes non-stereotyping dialect treatment and story-level consistency rules for the draft and editor.
- Batch size counts translatable pages. OCR regions inside one detected speech bubble are grouped into one translation unit; unmatched regions remain individual units. Fast page groups flatten those units into one ordered provider list; DeepL sends native text-array requests, splitting only at its 128 KiB request limit, and maps ordered responses back to their regions. Translation starts only after every active page completes OCR, bubble detection, and text grouping; downstream mask/layout/inpainting/render stages also wait for the full preceding stage.
- Batch checkpoints persist detection, OCR, bubble geometry, text grouping, translation, mask, layout, inpainting, and rendering separately. Source pixels stay on disk during translation and are loaded from the page checkpoint as later stages need them; each page releases decoded images and intermediate arrays after its stage.
- Default English rendering font resolution (`get_default_eng_font()`) prioritizes Wild Words (`Wild Words.ttf`, `CC Wild Words Roman.ttf`, `wild_words.ttf`, etc.) when present in `fonts/`, falling back gracefully to bundled comic fonts (`anime_ace.ttf` / `comic shanns 2.ttf`).
- Rendering adds one pixel to white character outlines over dark sampled source backgrounds; matching layout masks reserve the same width.
- Translated-text placement runs after the response. Units with available translations are rendered even when flagged for review, with page-level baseline coordination and adaptive multi-line placement. Bubble containment and obstacle checks remain hard constraints; failed placements are suppressed and flagged, then their original pixels are restored.
- Final layout validation rasterizes and transforms planned output, including fallback crops and effective outline widths, then checks page, panel, bubble, and inter-region collisions. Invalid or empty frozen/composited output is suppressed and flagged for review; readability below 85% of calibrated source size requests review unless a font size was explicitly set.
- Review-flagged text regions are rendered onto the canvas across backend pipeline rendering and web studio preview/editor while retaining explicit review flags, reasons, and badge indicators for subsequent review, draft saving, and approval in the studio.
- Speech bubble layout operates directly on arbitrary 2D free-space masks rather than rigid bounding boxes: uses Euclidean distance fields (SDF) to compute medial axes and boundary clearances, calculates spatial mask moments ($C_M, \Sigma_M$) and text inertia tensors ($C_T, \Sigma_T$) for orientation and aspect alignment, employs DP line breaking across horizontal span contours $C(y)$, and evaluates candidate layouts against centroid offset, inertia disparity, capacity utilization (~55–65% fill target with word-length bounds), raggedness, and linguistic boundaries. Candidate scoring accounts for FreeType ascender/descender metrics to avoid artificial box overflow, while prepared single-lobe and multi-lobe boxes composite directly into canvas points. Connected and multi-lobe bubbles balance word counts proportionally across spatial capacity while preserving uniform font sizing (with bounded per-lobe scaling when necessary), validating all glyph pixels against the safe shape with SDF margin clearance.
- Neural network model inference (detectors, OCR, bubble detection, inpainting, translators, upscalers) shares model instances through `SharedModelExecutor`; accelerator model calls are serialized, and GPU-capable stages wait for the accelerator slot instead of switching to CPU under contention.
- Production mask construction, shape-aware page layout, and text rendering use one process-wide bounded background CPU pool plus a separate interactive slot; batch rendering uses the background pool. OpenCV native threading is limited to one thread.
- Text detector checkpoints live under `models/detection/`; optional speech-bubble segmentation checkpoints live separately under `models/bubbles/`.
- Raw text detection bounding boxes (`detection.json`) and OCR textlines (`ocr.json`) are persisted immediately to disk and database storage upon stage completion, guaranteeing access to raw segmentation data prior to any downstream bubble grouping or translation filtering.
- Text grouping retains Unicode letters and numbers independently of translation eligibility using a 3-tier classification model (`MEANINGFUL_NUMERIC`, `POSSIBLE_NUMERIC`, `NOISE_NUMERIC`): intentional numeric regions (matching units/symbols/brackets, multi-digits, within/near speech bubbles or adjacent to linguistic text) and review-flagged possible numeric regions carry a `preserve` policy through translation checkpoints, are excluded from fast/professional provider payloads, and remain in masking, layout, and rendering at their measured source font size when available. Noise artifacts are filtered out.
- The page-detail and preview image components display detector polygon outlines color-coded by confidence tier (emerald $\ge 85\%$, amber $65\%-84\%$, rose $< 65\%$) with interactive hover highlights and floating tooltips showing exact confidence percentages (e.g. `#4 · 94% · high`), eliminating visual clutter on multi-line Japanese text.
- The web studio exposes OCR model selection and OCR Minimum Confidence threshold (`customOcrProb` / `ocrMinConfidence`), mapped to `OcrConfig.prob` and persisted across settings, batch jobs, and pipeline manifest metadata; every registered OCR backend applies the setting to its final text-region output.
- Backend OCR defaults use the registered `48px_ctc` model; alternate OCR models remain selectable through the enum and shared web options.
- Manga OCR (`mocr`, `kha-white/manga-ocr-base`) downloads complete model assets directly into `./models/ocr/` and loads offline from that directory when present; `docker_prepare.py` supports preparing `mocr` via `--models ocr.mocr` (or `mocr`).
- Result metadata (`meta.json` and `pipeline_manifest.json`) captures comprehensive pipeline step parameters (detector, resolution, box threshold, unclip ratio, OCR model, OCR min confidence, bubble detection settings, inpainting parameters, rendering configurations, colorizer, and upscaler); the web studio `PageDetailModal` displays these in a tabbed resizable sidebar with four tabs — Pipeline Timing (started/ended/duration + per-stage breakdown), Professional Localization, Story Analysis, and Step Settings (translation details + all step parameters). The sidebar defaults to 480 px wide and is drag-resizable from its left edge (280–700 px range).
- The batch SSE stream sends changed full batch details to the Web Studio; loaded items update live without follow-up detail GETs, while REST detail reads occur on first expansion only.
- Manga summary job lists and open summary progress now share an SSE stream; the server publishes changed snapshots and clients fetch the full summary only once the job reaches a terminal state.
- Summary OCR holds detection, OCR, and text-line merge behind manga-wide stage barriers for all uncached pages; model inference still uses worker-sized batches, and a failed group falls back to the existing single-page path.
- Summary progress records how many manga pages have completed the active detection, OCR, or text-line merge step, counting cached pages as reused work and pages with no detected text as processed; Jobs and the open summary view show the active step and this count.
- Search Lab status and embedding progress also use a change-only SSE stream; explicit refresh still performs a one-time status read.
- Search Lab's collection filter distinguishes indexed from unindexed manga; each manga shows summary/page coverage and can have its embeddings removed without deleting Gallery content. Removal is blocked while an embedding job is active.
- Dropping or uploading loose image files loads them directly into the studio cards list without intermediate staging, while multi-archive drops retain volume arrangement before extraction. Manga detail gallery supports smooth proximity-accelerated vertical auto-scrolling when dragging pages near viewport edges during page reordering. Selecting multiple pages or multiple manga cards supports batch deletion with confirmation prompts and atomic cache and state invalidation.
- Translated text lettering case can be configured to original (mixed case), ALL CAPS (uppercase), or lowercase across CLI arguments (`--uppercase`, `--lowercase`, `--letter-case`) and server batches/Web Studio options; English typesetting segmenters preserve original case without hardcoded upper-casing.
- Page Detail region overlays reveal IDs on selection, provide copy-ID controls for translated text, original detector, and speech-bubble regions, reuse loaded speech-bubble detections across view toggles, and only offer image retry after a failed load.

## Project references

- `README.md`: setup, configuration, and user-facing usage.
- `CONTEXT.md`: canonical domain language.
- `PRODUCT.md`: product purpose and principles.
- `DESIGN.md`: web studio design system.

## Common checks

- Python: `pytest`
- Frontend: `cd front && npm test`
- Frontend types: `cd front && npm run typecheck`
- Frontend build: `cd front && npm run build`
- Saved-page AI debugging: `python devscripts/pipeline_case.py get <copied-page-url> --output /tmp/page-case.json` (legacy alias: `inspect`); `rerun <copied-page-url> --from input|detection|bubble_detection|translation|layout --patch /tmp/pipeline-patch.json --render-output /tmp/pipeline-preview.jpg` executes against a temporary filesystem copy. It reads saved database documents without writing page or batch records; scratch files are removed automatically, while the review image and JSON output are retained at their requested paths.

## Working-tree note

The repository had extensive ongoing changes when this snapshot was created. Always inspect the current Git status and preserve unrelated work rather than assuming a clean checkout.
